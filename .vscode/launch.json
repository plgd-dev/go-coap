{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run simple server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/examples/observe/server"
        }
    ]
}