# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
debug
server
!server/
client
!client/
vendor/
v3/
/.idea/
# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# OpenSSL example certificates and keys
examples/dtls/pki/*.pem
examples/dtls/pki/*.csr
examples/dtls/pki/*.srl
